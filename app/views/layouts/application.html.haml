!!!
%html
  %head
    = render 'head'
  
  %body
    = render 'navbar'
    #wrapper
      
      %section#sidebar{role: :navigation}
        = sidebar_menu
      
      %section#tools
        = render_breadcrumbs
      
      %section#content
        .container-fluid
          
          = render partial: 'shared/flash'

          .hidden-xs-down
            = render_index_addnew
            = render_show_edit
            = render_edit_cancel

          %h1.page-title= yield :page_title

          = yield
          
      %section#footer
        %section.pagination
          = yield :pagination
          
    = yield :charts_js